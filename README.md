# datafun-01-utils

## Environment Setup (Required uv Process)

This project follows the required **uv** workflow for environment management.

### Create the virtual environment
```powershell
uv venv

